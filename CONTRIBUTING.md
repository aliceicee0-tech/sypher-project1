# Contributing

`main` is the default, protected-by-convention branch. Do feature work on a
branch (e.g. `feat/...`) and open a merge request targeting `main`.

See [README.md](./README.md) for setup and run instructions.
